namespace DidiFrame.Modals
{
	/// <summary>
	/// Context of modal submit
	/// </summary>
	/// <param name="Arguments">Object that contains components values</param>
	public record ModalSubmitContext(ModalArgumentsScope Arguments);
}
