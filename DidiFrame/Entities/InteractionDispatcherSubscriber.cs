namespace DidiFrame.Entities
{
	/// <summary>
	/// Delegate that subscribes handlers to interaction dispatcher 
	/// </summary>
	/// <param name="dispatcher">Dispatcher for subscriber</param>
	public delegate void InteractionDispatcherSubscriber(IInteractionDispatcher dispatcher);
}
